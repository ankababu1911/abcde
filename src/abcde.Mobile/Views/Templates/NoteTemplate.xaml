<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             BackgroundColor="{StaticResource PageBgColor}"
             x:Class="abcde.Mobile.Views.Templates.NoteTemplate">
    <Border StrokeThickness="1" Stroke="#e1e1e1" BackgroundColor="White">
        <Border.StrokeShape>
            <RoundRectangle CornerRadius="5"/>
        </Border.StrokeShape>
        <Border.Shadow>
            <Shadow Radius="1"
                    Offset="5,5"
                    Brush="WhiteSmoke"/>
        </Border.Shadow>
        <Grid Padding="20,20">
            <Label Text="{Binding NoteText,Mode=TwoWay}" FontSize="16" LineBreakMode="WordWrap" TextColor="#000" VerticalOptions="Center"/>
        </Grid>
    </Border>
</ContentView>
